# AmoCRM Integration Service

Микросервис для интеграции веб-сайтов с AmoCRM CRM-системой. Позволяет автоматически создавать контакты и сделки в AmoCRM через веб-формы на сайте.

## 📋 Описание проекта

Данный проект представляет собой TypeScript/Express микросервис, который обеспечивает:

- **Интеграцию с AmoCRM API** через официальный SDK
- **Обработку данных веб-форм** с различных сайтов
- **Создание контактов и сделок** в AmoCRM
- **Валидацию входящих данных** на серверной стороне
- **Поддержку множественных сайтов** с уникальными API ключами
- **CORS настройки** для безопасности

## ✨ Функциональные возможности

### Основные возможности:
- **Создание контактов** в AmoCRM с автоматическим разбором имени на имя и фамилию
- **Создание сделок** с привязкой к контактам
- **Поддержка кастомных полей**: телефон, email, компания, сообщение
- **Валидация данных** на серверной стороне с понятными сообщениями об ошибках
- **Многосайтовая архитектура** с отдельными API ключами для каждого сайта
- **CORS защита** с настройкой разрешенных доменов

### Поддерживаемые данные форм:
- **Имя** (обязательное, минимум 2 символа)
- **Телефон** (обязательное, с валидацией формата)
- **Email** (опциональное, с валидацией формата)
- **Компания** (опциональное)
- **Сообщение** (опциональное)

## 🚀 Установка и настройка

### Системные требования:
- Node.js 18+ 
- npm или yarn
- Доступ к AmoCRM аккаунту

### Установка зависимостей:
```bash
npm install
```

### Настройка переменных окружения:

1. Скопируйте файл `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Заполните необходимые переменные в файле `.env`:

```env
# Порт сервера
PORT=3001

# API ключи для сайтов
SITE1_ID="Название сайта 1"
SITE1_API_KEY=site1_key_abc123
SITE2_ID="Название сайта 2" 
SITE2_API_KEY=site2_key_def456
SITE3_ID="Название сайта 3"
SITE3_API_KEY=site3_key_ghi789

# Все ключи через запятую
API_KEYS=site1_key_abc123,site2_key_def456,site3_key_ghi789

# AmoCRM настройки
AMO_CRM_CLIENT_ID=your_client_id
AMO_CRM_CLIENT_SECRET=your_client_secret
AMO_CRM_REDIRECT_URI=http://localhost:3001/auth/callback
AMO_CRM_CLIENT_LTSTOKEN=your_long_term_token
AMO_CRM_SUBDOMAIN=your_subdomain

# ID кастомных полей в AmoCRM
AMO_CRM_MESSAGE_FIELD_ID=123456
AMO_CRM_COMPANY_FIELD_ID=789012
```

### Создание кастомных полей в AmoCRM:

1. **Поле "Компания" для контактов**:
   - Перейдите в Настройки → Контакты → Поля
   - Создайте поле типа "Текст"
   - Сохраните ID поля в `AMO_CRM_COMPANY_FIELD_ID`

2. **Поле "Сообщение" для сделок**:
   - Перейдите в Настройки → Сделки → Поля  
   - Создайте поле типа "Текст"
   - Сохраните ID поля в `AMO_CRM_MESSAGE_FIELD_ID`

### Запуск проекта:

**В режиме разработки:**
```bash
npm run dev
```

**Сборка и запуск:**
```bash
npm run build
npm start
```

## 🔗 Интеграция с фронтендом (пошаговый план)

### Шаг 1: Подключение JavaScript файла

Добавьте файл `site-integration.js` на ваш сайт:

```html
<!-- В секции <head> или перед закрывающим </body> -->
<script src="path/to/site-integration.js"></script>
```

### Шаг 2: Настройка формы

Добавьте атрибут `data-amocrm` к вашей форме:

```html
<form data-amocrm>
  <input type="text" name="name" placeholder="Имя" required>
  <input type="tel" name="phone" placeholder="Телефон" required>
  <input type="email" name="email" placeholder="Email">
  <input type="text" name="company" placeholder="Компания">
  <textarea name="message" placeholder="Сообщение"></textarea>
  <button type="submit">Отправить заявку</button>
</form>
```

### Шаг 3: Настройка JavaScript конфигурации

В файле `site-integration.js` обновите секцию `CONFIG`:

```javascript
const CONFIG = {
  apiUrl: 'https://ваш-домен.com/api/amo-crm', // URL вашего сервера
  apiKey: 'site1_key_abc123', // Ключ из .env
  siteName: 'Название вашего сайта'
};
```

### Шаг 4: Добавление домена в CORS настройки

В файле `src/index.ts` добавьте ваш домен в массив `allowedOrigins`:

```typescript
const allowedOrigins = [
  'http://localhost:3000',       // React/Vue dev сервер
  'https://ваш-сайт.com',        // Ваш production домен
  'https://другой-сайт.com',     // Дополнительные сайты
];
```

### Шаг 5: Кастомизация селекторов полей

При необходимости измените селекторы полей в `site-integration.js`:

```javascript
const fields = {
  'name': form.querySelector('[name="name"], [name="fio"], .name-field'),
  'phone': form.querySelector('[name="phone"], [name="tel"], .phone-field'),
  'email': form.querySelector('[name="email"], .email-field'),
  'company': form.querySelector('[name="company"], .company-field'),
  'message': form.querySelector('[name="message"], [name="comment"], textarea')
};
```

## ⚙️ Конфигурация

### Переменные окружения для AmoCRM:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `AMO_CRM_CLIENT_ID` | ID интеграции из AmoCRM | `12345` |
| `AMO_CRM_CLIENT_SECRET` | Секретный ключ интеграции | `secret_key` |
| `AMO_CRM_CLIENT_LTSTOKEN` | Долгосрочный токен доступа | `long_term_token` |
| `AMO_CRM_SUBDOMAIN` | Поддомен AmoCRM аккаунта | `yourcompany` |
| `AMO_CRM_REDIRECT_URI` | URL для OAuth редиректа | `http://localhost:3001/auth/callback` |
| `AMO_CRM_MESSAGE_FIELD_ID` | ID поля "Сообщение" для сделок | `123456` |
| `AMO_CRM_COMPANY_FIELD_ID` | ID поля "Компания" для контактов | `789012` |

### Переменные окружения для сервера:

| Переменная | Описание | Значение по умолчанию |
|------------|----------|---------------------|
| `PORT` | Порт сервера | `3001` |
| `NODE_ENV` | Режим работы | `development` |
| `API_KEYS` | Список валидных API ключей | - |
| `SITE{1,2,3}_ID` | Названия сайтов | - |
| `SITE{1,2,3}_API_KEY` | API ключи для сайтов | - |

## 📡 API методы

### GET /

Проверка статуса сервиса.

**Ответ:**
```json
{
  "service": "AmoCRM Integration Microservice",
  "status": "running", 
  "version": "1.0.0",
  "endpoints": {
    "createLead": "POST /api/amo-crm",
    "health": "GET /health"
  }
}
```

### GET /health

Проверка здоровья сервиса.

**Ответ:**
```json
{
  "status": "OK",
  "timestamp": "2023-12-19T18:09:30.074Z",
  "service": "amocrm-service"
}
```

### POST /api/amo-crm

Создание контакта и сделки в AmoCRM.

**Заголовки:**
- `Content-Type: application/json`
- `X-API-Key: {ваш_api_ключ}`

**Тело запроса:**
```json
{
  "name": "Иван Петров",
  "phone": "+7 (999) 123-45-67",
  "email": "ivan@example.com", 
  "company": "ООО Компания",
  "message": "Хочу получить консультацию"
}
```

**Успешный ответ (200):**
```json
{
  "success": true,
  "contactId": 12345,
  "message": "Контакт и сделка успешно созданы в AmoCRM"
}
```

**Ошибка валидации (400):**
```json
{
  "success": false,
  "errors": [
    "Имя обязательно и должно содержать минимум 2 символа",
    "Номер телефона обязателен и должен содержать только цифры и специальные символы"
  ]
}
```

**Ошибка авторизации (401):**
```json
{
  "success": false,
  "error": "Неверный API ключ"
}
```

**Ошибка сервера (500):**
```json
{
  "success": false,
  "error": "Внутренняя ошибка сервера",
  "details": "Подробности ошибки (только в development режиме)"
}
```

## 💡 Примеры использования

### Пример интеграции с React:

```jsx
import React, { useState } from 'react';

const ContactForm = () => {
  const [loading, setLoading] = useState(false);
  const [message, setMessage] = useState('');

  const handleSubmit = async (e) => {
    e.preventDefault();
    setLoading(true);
    setMessage('');

    const formData = new FormData(e.target);
    const data = Object.fromEntries(formData);

    try {
      const response = await fetch('/api/amo-crm', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'X-API-Key': 'site1_key_abc123'
        },
        body: JSON.stringify(data)
      });

      const result = await response.json();
      
      if (result.success) {
        setMessage('Заявка успешно отправлена!');
        e.target.reset();
      } else {
        setMessage(result.error || 'Ошибка при отправке');
      }
    } catch (error) {
      setMessage('Ошибка соединения. Попробуйте позже.');
    } finally {
      setLoading(false);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <input name="name" placeholder="Имя" required />
      <input name="phone" placeholder="Телефон" required />
      <input name="email" placeholder="Email" />
      <button type="submit" disabled={loading}>
        {loading ? 'Отправка...' : 'Отправить'}
      </button>
      {message && <div>{message}</div>}
    </form>
  );
};
```

### Пример интеграции с простым HTML:

```html
<!DOCTYPE html>
<html>
<head>
  <title>Форма обратной связи</title>
</head>
<body>
  <form id="contact-form" data-amocrm>
    <input type="text" name="name" placeholder="Имя" required>
    <input type="tel" name="phone" placeholder="Телефон" required>
    <input type="email" name="email" placeholder="Email">
    <textarea name="message" placeholder="Сообщение"></textarea>
    <button type="submit">Отправить</button>
  </form>

  <script src="site-integration.js"></script>
</body>
</html>
```

### Пример тестирования через curl:

```bash
curl -X POST http://localhost:3001/api/amo-crm \
  -H "Content-Type: application/json" \
  -H "X-API-Key: site1_key_abc123" \
  -d '{
    "name": "Иван Петров",
    "phone": "+7 (999) 123-45-67",
    "email": "ivan@example.com",
    "company": "ООО Пример",
    "message": "Тестовая заявка"
  }'
```

## 🔧 Техническая архитектура

### Структура проекта:

```
amocrm-integration/
├── src/
│   ├── index.ts                 # Точка входа Express сервера
│   ├── config/
│   │   └── amocrm.config.ts     # Конфигурация AmoCRM клиента
│   ├── services/
│   │   └── amocrm.service.ts    # Основной сервис работы с AmoCRM
│   └── utils/
│       └── validators.ts        # Валидация входящих данных
├── site-integration.js          # JavaScript для фронтенд интеграции
├── package.json                 # Зависимости и скрипты
├── tsconfig.json               # Конфигурация TypeScript
├── nodemon.json               # Конфигурация nodemon
├── .env.example               # Пример переменных окружения
└── README.md                  # Документация
```

### Основные компоненты:

1. **Express сервер** (`src/index.ts`):
   - API endpoints
   - CORS настройки
   - API key аутентификация
   - Валидация и обработка ошибок

2. **AmoCRM сервис** (`src/services/amocrm.service.ts`):
   - Создание контактов и сделок
   - Работа с кастомными полями
   - Обработка ошибок AmoCRM API

3. **Валидаторы** (`src/utils/validators.ts`):
   - Проверка обязательных полей
   - Валидация форматов email и телефона
   - Сообщения об ошибках на русском языке

4. **JavaScript интеграция** (`site-integration.js`):
   - Автоматическая обработка форм
   - Валидация на клиентской стороне
   - Показ сообщений пользователю

## 🛠 Разработка

### Доступные скрипты:

- `npm run dev` - Запуск в режиме разработки с nodemon
- `npm run build` - Сборка TypeScript в JavaScript
- `npm start` - Запуск production версии
- `npm test` - Запуск тестов (не настроен)

### Технологический стек:

- **Runtime**: Node.js
- **Язык**: TypeScript
- **Framework**: Express.js
- **AmoCRM SDK**: amocrm-js v3.6.0
- **HTTP клиент**: axios
- **Валидация**: custom validators
- **CORS**: cors middleware

## 📞 Поддержка

Для получения помощи или сообщения об ошибках:

1. Проверьте логи сервера на наличие ошибок
2. Убедитесь в правильности настройки переменных окружения
3. Проверьте подключение к AmoCRM API
4. Убедитесь в корректности API ключей

## 📝 Лицензия

ISC License - подробности в файле LICENSE.

---

# Улучшить

- проверку на существующий контакт
- создание компании вместо добавления поля Компания к контанкту